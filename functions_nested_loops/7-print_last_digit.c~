#include <stdio.h>
#include <stdlib.h>
#include "main.h"
/**
 *_abs - Entry point
 *@sigma: The input number
 *Return: Always 0
 */
int _abs(int sigma)
{
if (sigma >= 0)
return (sigma);
else
return (-sigma);
}
