#include <stdio.h>
#include "main.h"

/**
 * main - main
 * @argc: argv number
 * @argv: array
 * Return: Always 0 success
 */

int main(int argc, char *argv[])
{
	if (argc)
		printf("%s\n", argv[0]);

	return (0);
}
